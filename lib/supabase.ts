// Import from mock file which handles both real and mock Supabase
export { supabase, type Database } from './supabase-mock';

// Re-export everything for compatibility
export * from './supabase-mock';